# include <stdlib.h>
# include <stdint.h>
# include <unistd.h>

void add (struct cpu cp, uint32_t opcode);
void adda(struct cpu cpu, uint32_t opcode);//non implémenté
void addi(struct cpu cpu, uint32_t opcode);//non implémenté

void and (struct cpu cpu, uint32_t opcode);//non implménté
void andi (struct cpu cpu, uint32_t opcode);//non fonctionnel

void move (struct cpu cpu, uint32_t opcode);//non fonctionnel
